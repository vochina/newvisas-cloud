{
    "name": "newvisas-cloud",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "wrangler d1 migrations apply newvisas-cloud-db",
        "db:migrate:local": "wrangler d1 migrations apply newvisas-cloud-db --local",
        "db:studio": "drizzle-kit studio"
    },
    "dependencies": {
        "@hono/zod-validator": "^0.7.6",
        "drizzle-orm": "^0.38.0",
        "hono": "^4.6.0",
        "zod": "^4.3.5"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241218.0",
        "drizzle-kit": "^0.30.0",
        "typescript": "^5.7.0",
        "wrangler": "^4.0.0"
    }
}
